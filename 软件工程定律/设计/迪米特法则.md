---
title: 迪米特法则
category: 设计
description: 对象应尽量少了解别人的内部细节
---

## 什么是迪米特法则

迪米特法则常被概括为“最少知识原则”，意思是一个模块或对象应尽量少依赖其他模块的内部结构，而是通过清晰边界和必要接口协作。了解越少，耦合越低；耦合越低，内部实现变化时受影响的地方就越少。

## 核心要点

- 跨模块依赖越深入，改动波及面越大。
- 通过稳定接口协作，比直接穿透内部结构更可维护。
- 减少知道的细节，本质上是在限制耦合传播。

## 例子

订单服务为了读取用户优惠信息，最初直接一路访问 `user.profile.membership.couponPolicy.rules` 这样的深层结构。短期很方便，后来用户域重构数据模型时，订单服务一连串地方都跟着报错。问题不在于某个字段改名，而在于订单服务知道了太多本不该知道的内部细节。之后团队改成由用户域提供一个明确的“获取当前可用优惠策略”接口，订单侧只依赖结果，不再依赖内部结构。

## 适用场景

适合用在领域边界设计、服务接口封装、前后端组件协作和大型对象模型治理中。它特别适合那些一改数据结构就牵动大量调用方的场景。

## 常见误解

常见误解是把它理解成“不能访问任何其他模块”。法则并不反对协作，而是反对不必要地了解对方内部实现。

## 局限性与注意事项

过度追求“最少知识”也可能导致包装层过多、调用链变长。关键是平衡边界清晰和使用成本，而不是为了隔离而隔离。
